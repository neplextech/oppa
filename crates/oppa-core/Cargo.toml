[package]
name = "oppa-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Dependency-light domain primitives shared by OPPA crates"
readme = "README.md"

[dependencies]
chrono = { version = "0.4.45", features = ["serde"] }
serde = { version = "1.0.229", features = ["derive"] }
thiserror = "2.0.19"

[dev-dependencies]
serde_json = "1.0.145"
